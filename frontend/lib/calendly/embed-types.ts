/** UTM params appended to Calendly scheduling URLs (thin module — no embed-url dependency). */
export type CalendlyUtmParams = {
  utm_source?: string;
  utm_medium?: string;
  utm_campaign?: string;
  utm_content?: string;
};
