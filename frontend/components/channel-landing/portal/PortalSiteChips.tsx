'use client';

import type { ChannelLandingPage } from '@/types/channelLandingPage';

type Props = {
  page: ChannelLandingPage;
};

/** Store / Membership live in {@link PortalHeaderUtilities} only — no duplicate chip row. */
export default function PortalSiteChips(_props: Props) {
  return null;
}
