'use client'

/**
 * @deprecated Value items render in ChannelPortalQualification only. Kept so old imports do not duplicate sections.
 */
export default function ChannelPortalValueCards() {
  return null
}
