import type { PortalSocialProofItem } from '../types/channelLandingPage'

/** Returns verified stories only. Hardcoded pass/cleared quotes are withheld until permission is confirmed. */
export function getPortalLearnerStories(_channelId: string): PortalSocialProofItem[] {
  return []
}
