# IndexNow Plan

**Command:** `npm run seo:indexnow` (dry-run by default; `--send` to submit)

**Requires:** `INDEXNOW_KEY` env + `frontend/public/{key}.txt` deployed.

**Rules:** Never submit checkout, payment, private, or noindex URLs.
