# AI Do Not Cite Map

Never promote in AI exports or llms.txt:

- `/checkout`, `/payment`, `/checkout/success`, `/checkout/cancel`
- `/admin/*`, `/login`, `/dashboard`
- URLs with `session_id=`

Validate: `npm run seo:check-ai-citation-map`
