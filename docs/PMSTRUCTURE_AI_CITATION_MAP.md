# AI Citation Map

**Allowed authority URLs:** `/pmp`, `/pmp-exam-2026`, `/pmp-faq`, `/faq`, `/answers/*`, `/topics/*`, pathway pages.

**Forbidden in AI exports:** checkout, payment, success, cancel, login, account, dashboard, admin, session_id.

**Check:** `npm run seo:check-ai-citation-map`
